(function ($, app) {
	'use strict';

	// Start the application
	$(document).ready(function () {
		// Start the application
		console.log('We can start now our application !');

		app.start();
	});
}(jQuery, window.app));